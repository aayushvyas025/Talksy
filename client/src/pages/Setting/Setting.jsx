import Layout from "@/Layout/Layout";
import React, { Suspense } from "react";

function Setting() {
  return <Suspense>
    <Layout>
      Setting Page 
    </Layout>
  </Suspense>;
}

export default Setting;
